{{#if (isArrayType typeDto)}}
{{{getDataType typeDto}}};
{{else}}
{
{{#each (getProperties typeDto)}}
{{#if (isRequiredProperty this)}}
  {{{getValue name}}}: {{#or (eq (getTypeFromPropertyCSharp this) "object")}}{{> dtoPartial typeDto=range }}{{else}}{{{getTypeFromPropertyCSharp this}}}{{/or}};
{{else if (isOptionalProperty this)}}
  {{{getValue name}}}?: {{#or (eq (getTypeFromPropertyCSharp this) "object")}}{{> dtoPartial typeDto=range }}{{else}}{{{getTypeFromPropertyCSharp this}}}{{/or}};
{{/if}}
{{/each}}
}
{{~#if (isAdditionalPropertiesAllowed typeDto)}}
 & { [key: string]: any }
{{/if}}
{{/if}}
